# AGENTS.md

This file provides guidance to WARP (warp.dev) when working with code in this repository.

## Current repository state
- The repository has no commits yet (`master` branch, empty working tree).
- There are currently no source files, dependency manifests, or project-level docs (no `README.md`, `AGENTS.md`, `WARP.md`, `CLAUDE.md`, Cursor rules, or Copilot instructions were found before this file was created).

## Development commands
- No build, lint, or test commands are configured yet.
- No single-test workflow exists yet.
- Until project files are added, only repository-status commands are relevant:
  - `git --no-pager status --short --branch`
  - `git --no-pager ls-files`

## Architecture overview
- No application architecture exists yet because no code has been added.
- After the initial project scaffold is committed, update this file with:
  - canonical build/lint/test commands
  - single-test execution command
  - high-level module boundaries and runtime/data flow
